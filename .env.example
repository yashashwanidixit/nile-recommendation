# Server Configuration
HOST=127.0.0.1
PORT=8000
ENVIRONMENT=development
DEBUG=false

# CORS Configuration (JSON array format)
CORS_ORIGINS=["*"]

# Dataset File Paths (relative to workspace or absolute)
HOTELS_DATA_PATH=data/hotels.json
ACTIVITIES_DATA_PATH=data/activities.json

# LLM Provider Configuration (Reserved for future LLM integration stage)
# Provider and model selection are currently undecided and optional.
LLM_API_KEY=
LLM_MODEL=
